# 文件传输

## sftp
sftp user@remote_host
sftp> get /path/to/remote/file.txt /path/to/local/
sftp> get -r /path/to/remote/folder /path/to/local/

## SCP
scp test.txt user@remote_host:/home/user/
scp user@remote_host:/home/user/test.txt /home/localuser/
scp -r myfolder user@remote_host:/home/user/

## 文件同步 rsync
rsync -avz --delete /home/user/myfolder/ user@remote_host:/home/user/myfolder/
rsync -avz --delete  user@remote_host:/home/user/myfolder/
如果需要排除路径
rsync -av --exclude 'dir1/' --exclude 'dir2/' /path/to/source/ user@remote:/path/to/dest/
### 权限管理设置
rsync -avpogAXH --chown=:目标组名 /home/data/project/ user1@remote_host:/data/shared_folder/

### 软连接
#### -avL 可以直接把软连接链接到的实际图片发送到指定服务器
rsync -avL SRC_DIR/ user@remote:/path/on/remote/

## 备注
ssh-rsa验证
-oHostKeyAlgorithms=+ssh-rsa (scp)
-e "ssh -oHostKeyAlgorithms=+ssh-rsa -oPubkeyAcceptedAlgorithms=+ssh-rsa" (rsync)



